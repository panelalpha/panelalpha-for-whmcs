# Suggested Changelog (Staging)

This changelog summarizes the changes currently present on the `staging` branch when compared to the latest shared base with `main`.

## Added

- Added support for **DNS Manager for WHMCS** in PanelAlpha product resources.

## Fixed

- Fixed saving the **"Ask for domain"** product plan setting from PanelAlpha plans.
- Improved email template creation flow to ensure templates are created consistently.
- Fixed user email sending logic in local API handling.
